#this project is simple crud application using react and laravel
#done by yamlak kassahun

#project set up

step 1 clone the repo
step 2 cd in to the client folder and run
       #npm install
step 3 cd into server folder and run 
       #composer install
step 4 create database and run migration 
       # php artisan migrate
step 5 run the projects
    for client run 
    #npm start
    for server
    #php artisan serve

